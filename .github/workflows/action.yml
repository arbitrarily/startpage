name: Git Pull on Master Push
on: [push]
jobs:
  git-pull:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git Pull from Master
      run: git pull origin master

    - name: Output message
    - run: echo "🎉 The deploy was triggered ${{ github.event_name }}."